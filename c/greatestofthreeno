#include<stdio.h>
int main()
{
    int a , b ,c;
    printf("Enter three no.\n");
    scanf("%d%d%d",&a,&b,&c);
    if (a>b)
    {
        if(a>c)
        printf("number1 is greatest");
        else
        printf("number3 is greatest");
    }
    else
    {
        if(b>c)
        printf("number2 is greatest");
        else
        printf("number3 is greatest");
    }
    
}